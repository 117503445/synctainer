version: '3'

tasks:
  default:
    desc: "The default task" 
    # deps: [deploy]
    cmds:
      - task: fc-deploy
      
  fc-build:
    cmds:
      - CGO_ENABLED=0 go build -o ./cmd/fc/app ./cmd/fc

  fc-deploy:
    dir: ./cmd/fc
    deps: [fc-build]
    cmds:
      - s deploy -y